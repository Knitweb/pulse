# FinField feed mirror: map api/feed/<path> onto index.php?p=<path> and keep
# the on-disk cache unreadable directly.
RewriteEngine On
RewriteRule ^_cache(/|$) - [F,L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.+)$ index.php?p=$1 [L,QSA]
Options -Indexes
